#Return an array where each zero value in the array is replaced by the largest odd value to the right of the zero in the array.
#If there is no odd value to the right of the zero, leave the zero as a zero
import arr as arr


def move_zero(arr, zeros_counts=None):
    non_zero_elements = (for num in array_list if num != 0)
    count = arr.count(0)
    return non_zero_elements + [0] * zeros_counts

array_list = [0, 4, 3, 8, 7, 0, 3, 6, 2]

result = move_zero(array_list)
print(result)
====================================


dict_stu = { [ { "name": "Alice", "scores": [85, 92, 88] },
               { "name": "RAJ", "scores": [79, 85, 90] },
               { "name": "Charls", "scores": [95, 100, 93] } ] }

dict_stu[]
